---
phase: 02-primary-valuation-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/screens/entry/SystemMetrics.tsx
  - src/components/screens/entry/AIPulse.tsx
  - src/components/screens/entry/SystemAssets.tsx
  - src/components/screens/entry/CategorySelect.tsx
  - src/routes/index.tsx
autonomous: true
requirements: [ENT-01, ENT-02, ENT-03, ENT-04, ENT-05, ENT-06, ENT-07]

must_haves:
  truths:
    - "Entry screen at / matches the Stitch design with 3-column layout (stats left, hero center, widgets right)"
    - "System metrics cards show hardcoded values (84.2 T-FLOPS, 1.4M ARC, +2.4%, 12ms) with count-up animations"
    - "User can type an asset description in the main TokenInput field"
    - "User can select one of 5 categories via segmented control buttons"
    - "Pressing INITIATE PROTOCOL submits the form, calls createValuation server function, and redirects to /valuation/$assetId"
    - "AI Pulse widget shows SYNCHRONIZED status with animated bars"
    - "System Assets panel shows RARE_ASSET_1 (+2.4%) and CORE_TOKEN (STABLE)"
    - "Background watermark ENTRY_PRTCL text visible at ~5% opacity"
  artifacts:
    - path: "src/components/screens/entry/SystemMetrics.tsx"
      provides: "Left sidebar with Compute Power and Token Liquidity metric cards"
      min_lines: 30
    - path: "src/components/screens/entry/AIPulse.tsx"
      provides: "AI Pulse status widget with animated frequency bars"
      min_lines: 20
    - path: "src/components/screens/entry/SystemAssets.tsx"
      provides: "System Assets panel with RARE_ASSET_1 and CORE_TOKEN status"
      min_lines: 15
    - path: "src/components/screens/entry/CategorySelect.tsx"
      provides: "Segmented control for 5 asset categories"
      min_lines: 25
    - path: "src/routes/index.tsx"
      provides: "Complete Entry screen composing all sub-components"
      min_lines: 80
  key_links:
    - from: "src/routes/index.tsx"
      to: "src/core/functions/valuation.functions.ts"
      via: "createValuation server function call on form submit"
      pattern: "createValuation"
    - from: "src/routes/index.tsx"
      to: "/valuation/$assetId"
      via: "router.navigate after successful submission"
      pattern: "navigate.*valuation"
---

<objective>
Build the complete Tactical Entry Protocol screen (Screen 1) at `/`, pixel-perfect to the Stitch reference design. This is the landing page where users see system metrics, enter an asset description, select a category, and submit for AI valuation.

Purpose: This is the primary entry point and first impression of the app. It must be visually stunning with animated metrics, tactical styling, and a functional form that stores data and redirects to the reveal screen.

Output: A fully interactive Entry screen with metric cards, AI pulse widget, system assets panel, category selector, token input, submit button, and background watermark -- all wired to the createValuation server function.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-primary-valuation-pipeline/02-CONTEXT.md
@DESIGN.md
@tactical_entry_protocol_animated/code.html
</context>

<interfaces>
<!-- Existing UI components from Phase 1 to compose with -->

From src/components/ui/MetricDisplay.tsx:
```tsx
interface MetricDisplayProps {
  label: string
  value: number
  suffix?: string
  prefix?: string
  decimals?: number
  duration?: number
  trend?: string
  trendColor?: 'primary' | 'secondary' | 'tertiary' | 'error'
  barValue?: number
  className?: string
}
export function MetricDisplay({ ... }: MetricDisplayProps)
// Renders animated count-up inside HexCard with border-l accent
```

From src/components/ui/TokenInput.tsx:
```tsx
interface TokenInputProps extends React.InputHTMLAttributes<HTMLInputElement> {
  label?: string
  icon?: React.ReactNode
}
export const TokenInput = forwardRef<HTMLInputElement, TokenInputProps>(...)
// Renders clipped-full input with floating label, ambient glow, scanning line
```

From src/components/ui/NeonButton.tsx:
```tsx
interface NeonButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>,
  VariantProps<typeof neonButtonVariants> {
  children: React.ReactNode
}
// variant: 'primary' | 'secondary' | 'ghost', size: 'sm' | 'md' | 'lg'
export function NeonButton({ ... }: NeonButtonProps)
```

From src/components/ui/StatusIndicator.tsx:
```tsx
interface StatusIndicatorProps {
  status: 'active' | 'stable' | 'warning' | 'error' | 'pending'
  label: string
}
export function StatusIndicator({ ... }: StatusIndicatorProps)
```

From src/components/ui/HexCard.tsx:
```tsx
interface HexCardProps {
  elevation?: 'low' | 'medium' | 'high' | 'highest'
  clip?: 'none' | 'tr' | 'bl' | 'full'
  glow?: 'none' | 'cyan' | 'accent'
  borderAccent?: 'primary' | 'secondary' | 'accent' | 'none'
  children: React.ReactNode
}
export function HexCard({ ... }: HexCardProps)
```

From src/components/ui/GlitchText.tsx:
```tsx
interface GlitchTextProps {
  text: string
  as?: 'h1' | 'h2' | 'h3' | 'h4' | 'span' | 'p'
  className?: string
  active?: boolean
}
export function GlitchText({ ... }: GlitchTextProps)
```

From src/components/animations/variants.ts:
```ts
export const fadeInUp = { hidden: { opacity: 0, y: 20 }, visible: { opacity: 1, y: 0, ... } }
export const staggerContainer = { hidden: {}, visible: { transition: { staggerChildren: 0.1 } } }
```

From src/core/functions/valuation.functions.ts (created in Plan 01):
```ts
export const createValuation = createServerFn({ method: 'POST' })
  .validator(createValuationSchema)
  .handler(async ({ data }) => { ... })
// Returns { id: string }
```

From src/core/schemas.ts (created in Plan 01):
```ts
export const createValuationSchema = z.object({
  description: z.string().min(1).max(500),
  category: z.enum(['collectible', 'art', 'tech', 'luxury', 'other']),
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build Entry screen composite components</name>
  <files>src/components/screens/entry/SystemMetrics.tsx, src/components/screens/entry/AIPulse.tsx, src/components/screens/entry/SystemAssets.tsx, src/components/screens/entry/CategorySelect.tsx</files>
  <read_first>
    - tactical_entry_protocol_animated/code.html (FULL reference -- the Stitch design this must match)
    - src/components/ui/MetricDisplay.tsx (reuse for stat cards)
    - src/components/ui/HexCard.tsx (reuse for card containers)
    - src/components/ui/StatusIndicator.tsx (reuse for AI status)
    - src/components/animations/variants.ts (reuse for stagger animations)
    - src/styles/app.css (design tokens and clip-path utilities)
  </read_first>
  <action>
Create directory `src/components/screens/entry/` via mkdir -p.

**1. `SystemMetrics.tsx`** -- Left sidebar stats matching Stitch lines 219-239.

Compose 2 MetricDisplay cards in a vertical stack with gap-6:
- Card 1: label="Compute Power", value=84.2, decimals=1, suffix="T-FLOPS", barValue=84
- Card 2: Uses HexCard directly (not MetricDisplay) because Token Liquidity has a unique segmented bar (4 segments, not a single progress bar). Label "Token Liquidity", value "1.4M", suffix "ARC". Below the value, render 4 equal-width div bars in a flex row with gap-1, heights h-3, each with varying secondary opacity: secondary/40, secondary/60, secondary, secondary/20.

Use `motion.div` wrapper with `staggerContainer` and `fadeInUp` variants for entrance animation.

The component accepts no props -- all values are hardcoded per the CONTEXT.md decision.

**2. `AIPulse.tsx`** -- Right sidebar AI status widget matching Stitch lines 278-289.

Uses HexCard (elevation="low", clip="tr", borderAccent="primary").
- Label: "AI Pulse" (font-label text-[10px] text-outline uppercase tracking-widest)
- 6 animated vertical bars in a flex row (items-end, gap-1, h-12, mb-2). Each bar is w-2 with varying heights (30%, 60%, 90%, 50%, 40%, 20%) and primary-container opacity variants, each with `animate-pulse` and staggered animation-delay (0, 0.2s, 0.4s, 0.1s, 0.3s, 0.5s).
- Status text: "SYNCHRONIZED" (text-xl font-headline font-bold)
- Latency: "LATENCY: 12ms" (font-label text-[10px] text-primary-container)

**3. `SystemAssets.tsx`** -- Right sidebar assets panel matching Stitch lines 291-306.

Uses HexCard (elevation="highest", clip="tr", borderAccent="none").
- Gradient overlay: absolute inset-0 bg-gradient-to-br from-secondary/10 to-transparent
- Content in relative z-10 wrapper
- Label: "System Assets" (font-label text-[10px] text-outline uppercase tracking-widest mb-4)
- 2 rows in space-y-3:
  - Row 1: "RARE_ASSET_1" (text-on-surface-variant) | "+2.4%" (text-secondary font-bold)
  - Row 2: "CORE_TOKEN" (text-on-surface-variant) | "STABLE" (text-primary-container font-bold)
- Each row: flex justify-between items-center text-xs

**4. `CategorySelect.tsx`** -- Segmented control for 5 categories.

Props: `{ value: AssetCategory; onChange: (category: AssetCategory) => void }`

Import `AssetCategory` from `~/core/types`.

Render 5 category buttons in a flex row (flex-wrap gap-2). Each button:
- Text: category name uppercase (COLLECTIBLE, ART, TECH, LUXURY, OTHER)
- Base styles: px-4 py-2 font-headline text-xs font-bold uppercase tracking-widest transition-all duration-150
- Unselected: bg-surface-container-highest text-on-surface-variant hover:text-primary-container hover:bg-surface-container-high
- Selected: bg-primary-container/10 text-primary-container border-b-2 border-primary-container
- No rounded corners (tactical sharp edges)
- onClick calls onChange with the category value
  </action>
  <verify>
    <automated>ls src/components/screens/entry/SystemMetrics.tsx src/components/screens/entry/AIPulse.tsx src/components/screens/entry/SystemAssets.tsx src/components/screens/entry/CategorySelect.tsx && grep "84.2" src/components/screens/entry/SystemMetrics.tsx && grep "SYNCHRONIZED" src/components/screens/entry/AIPulse.tsx && grep "RARE_ASSET_1" src/components/screens/entry/SystemAssets.tsx && grep "AssetCategory" src/components/screens/entry/CategorySelect.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist in src/components/screens/entry/
    - SystemMetrics.tsx contains "84.2" (Compute Power value) and "1.4M" (Token Liquidity value)
    - SystemMetrics.tsx imports MetricDisplay from ~/components/ui/MetricDisplay
    - AIPulse.tsx contains "SYNCHRONIZED" and "LATENCY: 12ms"
    - AIPulse.tsx renders exactly 6 animated bars with animate-pulse
    - SystemAssets.tsx contains "RARE_ASSET_1", "+2.4%", "CORE_TOKEN", "STABLE"
    - CategorySelect.tsx accepts value: AssetCategory and onChange props
    - CategorySelect.tsx renders 5 buttons for collectible, art, tech, luxury, other
    - grep "animate-pulse" src/components/screens/entry/AIPulse.tsx returns matches
    - grep "onChange" src/components/screens/entry/CategorySelect.tsx returns matches
  </acceptance_criteria>
  <done>All 4 Entry screen composite components render with correct hardcoded values, tactical styling, and animations matching the Stitch reference</done>
</task>

<task type="auto">
  <name>Task 2: Build complete Entry screen route with form submission and navigation</name>
  <files>src/routes/index.tsx</files>
  <read_first>
    - src/routes/index.tsx (current placeholder -- will be fully replaced)
    - tactical_entry_protocol_animated/code.html (Stitch reference for overall layout)
    - src/components/screens/entry/SystemMetrics.tsx (just created)
    - src/components/screens/entry/AIPulse.tsx (just created)
    - src/components/screens/entry/SystemAssets.tsx (just created)
    - src/components/screens/entry/CategorySelect.tsx (just created)
    - src/components/ui/TokenInput.tsx (for the main input)
    - src/components/ui/NeonButton.tsx (for INITIATE PROTOCOL button)
    - src/components/ui/GlitchText.tsx (for the title)
    - src/core/functions/valuation.functions.ts (for createValuation server function)
    - src/core/types.ts (for AssetCategory type)
  </read_first>
  <action>
Replace the placeholder index.tsx with the full Entry screen. Reference the Stitch code.html layout (lines 216-308).

**Overall layout:** Container with 3-column flex on lg (stats left 1/4, hero center 2/4, widgets right 1/4). On mobile, single column with hero first (order-1), stats second (order-2), widgets third (order-3).

```
container mx-auto px-8 py-12 flex flex-col lg:flex-row gap-12 items-center justify-center
```

**Left column (lg:w-1/4, order-2 lg:order-1):**
- `<SystemMetrics />`

**Center column (lg:w-2/4, order-1 lg:order-2):**
- Title block (text-center mb-8):
  - h1: Use GlitchText with text="TACTICAL" followed by span.text-primary-container "ENTRY" followed by text " PROTOCOL". Actually since GlitchText renders a single text string, render the title manually with the text-glitch-hover class:
    ```
    <h1 className="font-headline font-black text-5xl md:text-7xl uppercase tracking-tighter leading-none mb-2 text-on-surface text-glitch-hover cursor-default">
      TACTICAL <span className="text-primary-container">ENTRY</span> PROTOCOL
    </h1>
    ```
  - Subtitle: `<p className="font-body italic text-lg text-outline max-w-md mx-auto">Harmonizing asset valuation through the Arcana lens. Submit the subject for extraction.</p>`

- Input group (w-full relative group):
  - Ambient glow div behind: `absolute -inset-4 bg-gradient-to-r from-primary-container/5 via-accent/5 to-primary-container/5 blur-2xl opacity-50 group-hover:opacity-100 transition-opacity`
  - TokenInput with label="SUBJECT_IDENTIFIER" and a pulsing token icon on the left
  - The icon should be a span with animate-pulse (match Stitch line 257): use an SVG or Unicode diamond character styled in primary-container
  - Placeholder text: "Enter asset description..."

- Category selector (mt-4):
  - `<CategorySelect value={category} onChange={setCategory} />`

- Submit button (mt-8 flex justify-center):
  - `<NeonButton variant="primary" size="lg" onClick={handleSubmit} disabled={isSubmitting}>INITIATE PROTOCOL</NeonButton>`
  - The button content should include a bolt icon (Unicode or SVG)

**Right column (lg:w-1/4, order-3):**
- `<AIPulse />`
- `<SystemAssets />` (with gap-6 between them in a flex-col)

**Background watermark** (bottom-left, fixed, pointer-events-none):
```
<div className="fixed bottom-0 left-0 p-12 pointer-events-none select-none z-0">
  <h2 className="font-headline font-black text-[12vw] leading-none text-on-surface/[0.05] uppercase tracking-tighter mix-blend-overlay">
    ENTRY_PRTCL
  </h2>
</div>
```

**Form state and submission logic:**
```tsx
const [description, setDescription] = useState('')
const [category, setCategory] = useState<AssetCategory>('other')
const [isSubmitting, setIsSubmitting] = useState(false)
const navigate = Route.useNavigate()  // or useNavigate() from @tanstack/react-router

async function handleSubmit() {
  if (!description.trim() || isSubmitting) return
  setIsSubmitting(true)
  try {
    const result = await createValuation({ data: { description, category } })
    navigate({ to: '/valuation/$assetId', params: { assetId: result.id } })
  } catch (error) {
    console.error('Valuation failed:', error)
    setIsSubmitting(false)
  }
}
```

Use `motion.div` wrappers with `fadeInUp` and `staggerContainer` variants on the main content sections for entrance animations. The container should use `staggerContainer` as `variants` on a `motion.div`, with each column as a `motion.div` with `fadeInUp` variants.

The route definition stays as `createFileRoute('/')` with `component: Home`.
  </action>
  <verify>
    <automated>grep "createValuation" src/routes/index.tsx && grep "INITIATE PROTOCOL" src/routes/index.tsx && grep "ENTRY_PRTCL" src/routes/index.tsx && grep "CategorySelect" src/routes/index.tsx && grep "SystemMetrics" src/routes/index.tsx && grep "AIPulse" src/routes/index.tsx && grep "navigate" src/routes/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/index.tsx imports and renders SystemMetrics, AIPulse, SystemAssets, CategorySelect
    - src/routes/index.tsx imports createValuation from ~/core/functions/valuation.functions
    - src/routes/index.tsx has useState for description, category, isSubmitting
    - src/routes/index.tsx calls createValuation on form submission
    - src/routes/index.tsx navigates to /valuation/$assetId after successful submission
    - src/routes/index.tsx contains "ENTRY_PRTCL" watermark text
    - src/routes/index.tsx contains "INITIATE PROTOCOL" button text
    - src/routes/index.tsx contains "TACTICAL" and "ENTRY" and "PROTOCOL" in the title
    - src/routes/index.tsx uses 3-column flex layout (lg:w-1/4, lg:w-2/4, lg:w-1/4)
    - src/routes/index.tsx contains TokenInput import
    - grep "text-primary-container.*ENTRY" src/routes/index.tsx returns a match
  </acceptance_criteria>
  <done>Entry screen at / renders pixel-perfect to Stitch design with working form submission that stores in D1, triggers AI, and redirects to /valuation/$assetId</done>
</task>

</tasks>

<verification>
- `pnpm dev` starts and the Entry screen renders at localhost:3000
- System metrics cards show count-up animations for 84.2 T-FLOPS and 1.4M ARC
- AI Pulse shows SYNCHRONIZED with animated bars
- System Assets shows RARE_ASSET_1 and CORE_TOKEN
- Category selector has 5 selectable options
- ENTRY_PRTCL watermark visible at bottom-left
- Form submission calls createValuation and navigates to /valuation/$assetId
</verification>

<success_criteria>
- All 5 files exist (4 components + 1 route)
- Entry screen layout matches 3-column Stitch reference design
- All hardcoded values present (84.2 T-FLOPS, 1.4M ARC, +2.4%, 12ms, SYNCHRONIZED)
- Form state management with description, category, isSubmitting
- createValuation server function called on submit
- Navigation to /valuation/$assetId after submission
- Background watermark ENTRY_PRTCL rendered
</success_criteria>

<output>
After completion, create `.planning/phases/02-primary-valuation-pipeline/02-02-SUMMARY.md`
</output>
