---
phase: 04-polish-and-production-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/__root.tsx
  - src/components/DefaultCatchBoundary.tsx
  - src/components/NotFound.tsx
  - src/routes/index.tsx
  - src/routes/ranking.tsx
  - src/routes/what-if.tsx
  - src/routes/terminal.tsx
  - src/routes/valuation.$assetId.tsx
  - src/components/layout/Shell.tsx
  - src/components/layout/Header.tsx
  - src/components/screens/whatif/LabParameters.tsx
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-03
  - DEPL-04

must_haves:
  truths:
    - "Navigating between any two screens shows a smooth fade + Y-translate transition"
    - "All 5 screens render correctly on both desktop and 768px tablet viewport"
    - "Submitting an asset valuation shows a pulseGlow loading skeleton while AI processes"
    - "If a route throws an error, a tactical SYSTEM ERROR HexCard with GlitchText appears with a REINITIALIZE retry button"
  artifacts:
    - path: "src/routes/__root.tsx"
      provides: "AnimatePresence wrapper around Outlet for page transitions"
      contains: "AnimatePresence"
    - path: "src/components/DefaultCatchBoundary.tsx"
      provides: "Tactical-themed error boundary with HexCard and GlitchText"
      contains: "SYSTEM ERROR"
    - path: "src/components/NotFound.tsx"
      provides: "Tactical-themed 404 page"
      contains: "SIGNAL LOST"
  key_links:
    - from: "src/routes/__root.tsx"
      to: "AnimatePresence"
      via: "wraps Outlet with motion.div for route transitions"
      pattern: "AnimatePresence"
    - from: "src/components/DefaultCatchBoundary.tsx"
      to: "src/components/ui/HexCard.tsx"
      via: "renders error inside HexCard with GlitchText"
      pattern: "HexCard"
---

<objective>
Add page transitions, responsive tablet layouts, loading states, and tactical-themed error boundaries to the complete app.

Purpose: Polish the feature-complete app for portfolio-quality presentation before deployment.
Output: Smooth page transitions on navigation, tablet-responsive layouts, loading skeletons on AI interactions, and on-brand error pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-and-production-deploy/04-CONTEXT.md

@src/routes/__root.tsx
@src/components/layout/Shell.tsx
@src/components/layout/Header.tsx
@src/components/DefaultCatchBoundary.tsx
@src/components/NotFound.tsx
@src/components/ui/HexCard.tsx
@src/components/ui/GlitchText.tsx
@src/components/ui/NeonButton.tsx
@src/components/animations/variants.ts
@src/routes/index.tsx
@src/routes/what-if.tsx
@src/routes/ranking.tsx
@src/routes/terminal.tsx
@src/routes/valuation.$assetId.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/ui/HexCard.tsx:
```typescript
interface HexCardProps extends React.HTMLAttributes<HTMLDivElement>,
    VariantProps<typeof hexCardVariants> {
  borderAccent?: 'primary' | 'secondary' | 'accent' | 'none'
}
// Variants: elevation (low|medium|high|highest), clip (none|tr|bl|full), glow (none|cyan|accent)
```

From src/components/ui/GlitchText.tsx:
```typescript
interface GlitchTextProps {
  text: string
  as?: 'h1' | 'h2' | 'h3' | 'h4' | 'span' | 'p'
  className?: string
  active?: boolean
}
```

From src/components/animations/variants.ts:
```typescript
export const pulseGlow = {
  animate: {
    boxShadow: ['0 0 20px rgba(143, 245, 255, 0.1)', '0 0 40px rgba(143, 245, 255, 0.3)', '0 0 20px rgba(143, 245, 255, 0.1)'],
    transition: { duration: 2, repeat: Infinity },
  },
}
```

From src/components/ui/NeonButton.tsx:
```typescript
// NeonButton with variant="primary"|"secondary"|"ghost", size="sm"|"md"|"lg"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Page transitions, error boundary, and loading states</name>
  <files>
    src/routes/__root.tsx,
    src/components/DefaultCatchBoundary.tsx,
    src/components/NotFound.tsx,
    src/routes/index.tsx,
    src/components/screens/whatif/ScenarioCard.tsx
  </files>
  <read_first>
    src/routes/__root.tsx,
    src/components/DefaultCatchBoundary.tsx,
    src/components/NotFound.tsx,
    src/routes/index.tsx,
    src/components/screens/whatif/ScenarioCard.tsx,
    src/components/ui/HexCard.tsx,
    src/components/ui/GlitchText.tsx,
    src/components/ui/NeonButton.tsx,
    src/components/animations/variants.ts,
    src/components/layout/Shell.tsx
  </read_first>
  <action>
    **Page transitions (per DEPL-01):**
    In `src/routes/__root.tsx`, import `AnimatePresence` and `motion` from `motion/react`. In the `RootComponent` function, wrap the `<Outlet />` with `<AnimatePresence mode="wait">` and a `<motion.div>` that uses `key={useRouterState().location.pathname}` for route-change detection. The motion.div should have:
    - `initial={{ opacity: 0, y: 10 }}`
    - `animate={{ opacity: 1, y: 0 }}`
    - `exit={{ opacity: 0, y: -10 }}`
    - `transition={{ duration: 0.3 }}`
    Import `useRouterState` from `@tanstack/react-router` (already used in Header, proven pattern).

    **Error boundary (per DEPL-04):**
    Replace the entire `DefaultCatchBoundary.tsx` with a tactical-themed error component. Import `HexCard`, `GlitchText`, and `NeonButton`. Render a centered layout with:
    - `<HexCard elevation="high" glow="accent" borderAccent="accent" clip="tr">` as the container
    - `<GlitchText text="SYSTEM ERROR" as="h1" active className="text-4xl text-error" />`
    - Error message in a `font-label text-xs text-on-surface-variant` div, prefixed with "ERR_TRACE: "
    - `<NeonButton variant="primary" onClick={() => router.invalidate()}>REINITIALIZE</NeonButton>` for retry
    - A "RETURN TO BASE" link using `<Link to="/">` below the button
    - Keep the existing `useRouter()` and `useMatch()` logic for root detection

    Replace the entire `NotFound.tsx` with a tactical-themed 404 page:
    - Same HexCard container pattern as error boundary but with `glow="cyan"` and `borderAccent="primary"`
    - `<GlitchText text="SIGNAL LOST" as="h1" active className="text-4xl text-primary-container" />`
    - Status text: "TARGET_NOT_FOUND // ROUTE DOES NOT EXIST"
    - NeonButton to go back and Link to "/"

    **Loading states (per DEPL-03):**
    In `src/routes/index.tsx`, update the `handleSubmit` function. When `isSubmitting` is true, instead of just changing button text to "PROCESSING...", also render a loading overlay below the form (before the background watermark). Use a `motion.div` with the `pulseGlow` variant from variants.ts wrapping a HexCard with:
    - `<HexCard elevation="medium" glow="cyan">`
    - Text "PROCESSING..." in `font-label text-xs text-primary-container animate-pulse`
    - Text "ARCANA ENGINE ANALYZING ASSET" in `font-label text-[10px] text-outline`
    Show this only when `isSubmitting` is true using conditional rendering with AnimatePresence for smooth enter/exit.

    The ScenarioCard in `src/components/screens/whatif/ScenarioCard.tsx` already has loading states ("GENERATING SCENARIO OUTPUT..." with animate-pulse). This is sufficient for DEPL-03 -- no changes needed there.
  </action>
  <verify>
    <automated>cd /Users/djbeatbug/RoadToMillion/token-for-granted && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AnimatePresence" src/routes/__root.tsx
    - grep -q "SYSTEM ERROR" src/components/DefaultCatchBoundary.tsx
    - grep -q "HexCard" src/components/DefaultCatchBoundary.tsx
    - grep -q "GlitchText" src/components/DefaultCatchBoundary.tsx
    - grep -q "REINITIALIZE" src/components/DefaultCatchBoundary.tsx
    - grep -q "SIGNAL LOST" src/components/NotFound.tsx
    - grep -q "PROCESSING" src/routes/index.tsx
    - grep -q "pulseGlow" src/routes/index.tsx
  </acceptance_criteria>
  <done>
    Page transitions animate fade+Y-translate(10px) over 300ms when navigating between routes. Error boundary shows tactical SYSTEM ERROR HexCard with GlitchText and REINITIALIZE retry button. NotFound shows tactical SIGNAL LOST page. Entry screen shows pulseGlow loading skeleton during AI submission.
  </done>
</task>

<task type="auto">
  <name>Task 2: Responsive tablet layouts at 768px breakpoint</name>
  <files>
    src/routes/index.tsx,
    src/routes/ranking.tsx,
    src/routes/what-if.tsx,
    src/routes/terminal.tsx,
    src/routes/valuation.$assetId.tsx,
    src/components/layout/Header.tsx,
    src/components/screens/whatif/LabParameters.tsx
  </files>
  <read_first>
    src/routes/index.tsx,
    src/routes/ranking.tsx,
    src/routes/what-if.tsx,
    src/routes/terminal.tsx,
    src/routes/valuation.$assetId.tsx,
    src/components/layout/Header.tsx,
    src/components/screens/whatif/LabParameters.tsx
  </read_first>
  <action>
    **Responsive design (per DEPL-02) -- desktop-first, tablet-friendly at 768px (md breakpoint):**

    This is desktop-first so existing layouts are the desktop view. Add responsive classes to make layouts stack/adapt at tablet (md: breakpoint and below). No mobile optimization -- just ensure nothing breaks below 768px.

    **Header (`src/components/layout/Header.tsx`):**
    Nav links already use `hidden md:flex` -- good. Ensure the title text scales down: add `text-lg md:text-2xl` to the TACTICAL ARCANA div. Reduce px padding on small: `px-4 md:px-6`.

    **Entry screen (`src/routes/index.tsx`):**
    Already has `flex flex-col lg:flex-row` and responsive widths. Verify `text-5xl md:text-7xl` on the title. Reduce container padding: `px-4 md:px-8`. Reduce `py-8 md:py-12`. These are minor tweaks -- the existing responsive classes are solid.

    **Ranking screen (`src/routes/ranking.tsx`):**
    Grid already uses `grid-cols-1 md:grid-cols-2 lg:grid-cols-4`. Reduce title size: `text-5xl md:text-8xl`. Reduce padding: `px-4 md:px-6 lg:px-12`. Reduce watermark: `text-6xl md:text-8xl lg:text-[12rem]`. Reduce top padding: `pt-20 md:pt-32`.

    **What If Lab (`src/routes/what-if.tsx`):**
    The LabParameters sidebar should collapse to a horizontal bar on tablet. In `src/components/screens/whatif/LabParameters.tsx`, change the sidebar from always-visible to `hidden lg:flex` as a side column, and add a condensed horizontal version that shows on `flex lg:hidden` as a top bar. The main content area wrapping div in `what-if.tsx` should change from `flex` to `flex flex-col lg:flex-row`. The scenario grid already uses `grid-cols-1 lg:grid-cols-3`. Reduce title: `text-3xl md:text-5xl`. Reduce padding: `p-6 md:p-12`.

    **Terminal screen (`src/routes/terminal.tsx`):**
    Read the full file. The terminal has a sidebar and main content area. Make sidebar `hidden lg:flex` on tablet (similar to what-if). Reduce padding and font sizes for tablet. The grid panels should stack: ensure `grid-cols-1 md:grid-cols-3` pattern. Reduce padding: `p-4 md:p-8`.

    **Valuation reveal (`src/routes/valuation.$assetId.tsx`):**
    Read the full file. Stack the asset card and metrics into single column on tablet. Ensure grid uses `grid-cols-1 md:grid-cols-2` for multiplier cards. Reduce title sizes and padding for md breakpoint.

    **General approach:** Only add/modify Tailwind responsive prefixes. Do NOT restructure component hierarchies. Keep all existing desktop styles as the base or lg: prefixed versions.
  </action>
  <verify>
    <automated>cd /Users/djbeatbug/RoadToMillion/token-for-granted && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "text-lg md:text-2xl" src/components/layout/Header.tsx
    - grep -q "px-4" src/routes/index.tsx
    - grep -q "hidden lg:flex" src/components/screens/whatif/LabParameters.tsx || grep -q "lg:flex" src/components/screens/whatif/LabParameters.tsx
    - grep -q "grid-cols-1" src/routes/ranking.tsx
  </acceptance_criteria>
  <done>
    All 5 screens render without layout breakage at 768px tablet viewport. Sidebars on What If Lab and Terminal collapse. Grids stack to fewer columns. Font sizes and padding reduce appropriately. Desktop layout is unchanged.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `pnpm build` completes successfully
3. Navigate between routes in dev mode -- smooth fade+slide transitions visible
4. Resize browser to 768px -- all screens render without horizontal overflow or broken layouts
5. Trigger an error (e.g., visit `/valuation/nonexistent`) -- tactical error card appears
</verification>

<success_criteria>
- AnimatePresence wraps Outlet in __root.tsx with fade+Y-translate(10px) 300ms transitions
- DefaultCatchBoundary renders tactical SYSTEM ERROR HexCard with GlitchText and REINITIALIZE button
- NotFound renders tactical SIGNAL LOST page
- Entry screen shows pulseGlow loading skeleton during AI submission
- All 5 screens render correctly at both desktop and 768px tablet widths
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-and-production-deploy/04-01-SUMMARY.md`
</output>
