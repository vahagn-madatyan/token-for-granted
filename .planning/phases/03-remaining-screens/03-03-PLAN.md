---
phase: 03-remaining-screens
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/terminal.tsx
  - src/components/screens/terminal/BootSequence.tsx
  - src/components/screens/terminal/SystemMonitor.tsx
  - src/components/screens/terminal/TerminalSidebar.tsx
autonomous: true
requirements: [TRM-01, TRM-02, TRM-03, TRM-04, TRM-05, TRM-06, TRM-07, TRM-08]

must_haves:
  truths:
    - "User sees the Terminal page at /terminal with command console, boot sequence, and monitoring panels"
    - "Boot sequence plays on first visit: ~8 themed log entries appear sequentially with staggered animation"
    - "Command console shows timestamped, color-coded log entries (INFO, WARNING, CRITICAL)"
    - "System Health panel shows processor load and aether consumption bars"
    - "Token Latency panel shows ARC_NET, VAL_FEED, and GLOBAL_REACH metrics that update periodically"
    - "Node Connections panel shows Paris HUB-01, Seoul LNK-04, NYC GATE-09, Tokyo DATA-11, London STR-02 with status cycling"
    - "Command input field with EXECUTE_LINK button shows easter egg response"
    - "Sidebar shows 4 navigation icons (Dashboard, Arsenal, Archives, Protocol)"
  artifacts:
    - path: "src/routes/terminal.tsx"
      provides: "Tactical Terminal screen with sidebar, console, and monitoring panels"
      contains: "createFileRoute"
    - path: "src/components/screens/terminal/BootSequence.tsx"
      provides: "Boot sequence hook and log generation logic"
      contains: "useBootSequence"
    - path: "src/components/screens/terminal/SystemMonitor.tsx"
      provides: "Right panel with System Health, Token Latency, Node Connections"
      contains: "SystemMonitor"
    - path: "src/components/screens/terminal/TerminalSidebar.tsx"
      provides: "Left icon sidebar matching Stitch design"
      contains: "TerminalSidebar"
  key_links:
    - from: "src/routes/terminal.tsx"
      to: "BootSequence hook"
      via: "useBootSequence hook provides log entries for the console"
      pattern: "useBootSequence"
    - from: "src/routes/terminal.tsx"
      to: "SystemMonitor component"
      via: "renders right panel with live metrics"
      pattern: "SystemMonitor"
    - from: "src/components/screens/terminal/SystemMonitor.tsx"
      to: "setInterval"
      via: "periodic metric updates every 2-3 seconds"
      pattern: "setInterval"
---

<objective>
Build the Tactical Terminal screen at /terminal with client-side boot sequence, timestamped color-coded log entries, decorative command input with easter egg, system health/token latency/node connections monitoring panels, and sidebar navigation icons.

Purpose: Delivers the Terminal monitoring console pixel-perfect to the Stitch reference. All data is client-side generated for theatrical effect.
Output: Fully animated Terminal page with BootSequence, SystemMonitor, and TerminalSidebar components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remaining-screens/03-CONTEXT.md
@tactical_terminal_animated/code.html
@src/components/ui/TerminalLog.tsx
@src/components/animations/variants.ts
@src/routes/index.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ui/TerminalLog.tsx:
```typescript
type LogLevel = 'INFO' | 'WARNING' | 'CRITICAL' | 'SUCCESS' | 'DEBUG'
interface LogEntry {
  timestamp: string
  level: LogLevel
  source: string
  message: string
}
interface TerminalLogProps {
  entries: LogEntry[]
  maxHeight?: string
  className?: string
}
export function TerminalLog({ entries, maxHeight, className }: TerminalLogProps)
export type { LogEntry, LogLevel }
```

From src/components/animations/variants.ts:
```typescript
export const fadeInUp = { hidden: { opacity: 0, y: 20 }, visible: { opacity: 1, y: 0, transition: { duration: 0.5 } } }
export const staggerContainer = { hidden: {}, visible: { transition: { staggerChildren: 0.1 } } }
```

From src/lib/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BootSequence hook, TerminalSidebar, and SystemMonitor components</name>
  <files>src/components/screens/terminal/BootSequence.tsx, src/components/screens/terminal/TerminalSidebar.tsx, src/components/screens/terminal/SystemMonitor.tsx</files>
  <read_first>
    - tactical_terminal_animated/code.html (full Stitch reference for all terminal panels)
    - src/components/ui/TerminalLog.tsx (LogEntry and LogLevel types to reuse)
    - src/lib/cn.ts (cn utility)
    - src/components/animations/variants.ts (animation variants)
  </read_first>
  <action>
Create `src/components/screens/terminal/` directory with 3 files:

**1. BootSequence.tsx:**
Export a custom hook `useBootSequence()` that returns `{ entries: LogEntry[], isBooting: boolean }`.

Import `LogEntry` type from `~/components/ui/TerminalLog`.

The hook:
- Defines 8 pre-written boot sequence entries matching the Stitch reference:
```typescript
const BOOT_ENTRIES: LogEntry[] = [
  { timestamp: '08:42:12', level: 'INFO', source: 'BOOT_SEQUENCE', message: 'Initializing Tactical Arcana heuristic engine...' },
  { timestamp: '08:42:15', level: 'INFO', source: 'CORE_STATUS', message: 'Loading assets from VAL_ARCHIVE_SECTOR_07... [OK]' },
  { timestamp: '08:42:21', level: 'WARNING', source: 'WARNING', message: 'Anomalous token detected in \'PHOENIX\' profile valuation. Recalibrating...' },
  { timestamp: '08:43:02', level: 'INFO', source: 'SYS_LOG', message: 'Processing asset: \'ORION_PRIME_VANDAL\' -- Current Valuation: 4,200 ARC -- Market Latency: 12ms -- Sentiment Score: 0.89 (BULLISH)' },
  { timestamp: '08:44:18', level: 'INFO', source: 'AI_FEED', message: 'Global volatility spike detected in Sector 4. Adjusting risk parameters.' },
  { timestamp: '08:45:01', level: 'INFO', source: 'SYS_LOG', message: 'Node connection \'SHA-2\' stabilized. Data throughput at 98%.' },
  { timestamp: '08:45:33', level: 'CRITICAL', source: 'CRITICAL', message: 'Unidentified protocol access attempt from external IP: 192.168.0.XX. Firewall holding.' },
  { timestamp: '08:46:55', level: 'INFO', source: 'CORE', message: 'Ready for operator instruction. Terminal awaiting command...' },
]
```
- Uses useState to track visible entries (starts at 0)
- Uses useEffect with sequential setTimeout: adds one entry every ~200ms (staggered boot feel)
- isBooting = true until all 8 entries are shown
- Also define ongoing log entries that appear AFTER boot completes -- add one new entry every 4-6 seconds via setInterval:
```typescript
const ONGOING_ENTRIES: LogEntry[] = [
  { timestamp: '08:47:22', level: 'INFO', source: 'NET_SCAN', message: 'Heartbeat check: all 5 nodes responding within tolerance.' },
  { timestamp: '08:48:01', level: 'SUCCESS', source: 'CACHE', message: 'KV cache hit ratio: 94.2% -- operating above threshold.' },
  { timestamp: '08:48:45', level: 'WARNING', source: 'AI_CORE', message: 'Model temperature drift detected: +0.03 above baseline. Auto-correcting.' },
  { timestamp: '08:49:12', level: 'INFO', source: 'VALUATION', message: 'New asset ingested: QUANTUM_SILK_SCARF -- Preliminary tier: B' },
  { timestamp: '08:50:00', level: 'DEBUG', source: 'METRICS', message: 'System uptime: 14h 22m 41s -- No restart required.' },
]
```
- After boot completes, starts cycling through ongoing entries with setInterval (random 4000-6000ms), generating timestamps dynamically using current time formatted as HH:MM:SS

**2. TerminalSidebar.tsx:**
Left sidebar matching the Stitch terminal design. No props needed.

Structure (matching tactical_terminal_animated/code.html sidebar):
- Container: `w-20 bg-surface-container-low flex flex-col items-center py-6 gap-8 border-r border-outline-variant/20` with clip-path: polygon(0% 0%, 100% 0%, 100% 95%, 80% 100%, 0% 100%)
- 4 icon buttons stacked vertically:
  1. DASHBOARD -- icon grid_view (use simple 4-square grid SVG or text "grid"), text-outline hover:text-secondary
  2. ARSENAL -- icon swords (use crossed swords SVG or text "swords"), text-outline hover:text-secondary
  3. ARCHIVES -- icon menu_book (use book SVG or text "book"), text-outline hover:text-secondary
  4. PROTOCOL -- active state: bg-primary-container/10, text-primary-container, border-l-4 border-primary-container. Icon "security" (use shield SVG or text "shield")
- Each button: `flex flex-col items-center gap-1 w-full py-2`, label in font-headline font-medium text-[8px] tracking-tighter
- Bottom: vertical text "UNITS: 04" in primary-container color, rotated 180deg

For icons: Since the project uses neither Material Symbols nor Lucide (checking if lucide-react is installed), use simple inline SVGs or stylized text representations. Keep it minimal -- the icons are small (text-2xl equivalent). Best approach: use simple Unicode or SVG paths.

**3. SystemMonitor.tsx:**
Right panel with 3 monitoring cards. No props needed (all client-side data).

Uses useState + useEffect with setInterval for live-feeling metrics.

Structure (matching the right aside in tactical_terminal_animated/code.html):
- Container: `w-80 flex flex-col gap-6`

**System Health card:**
- `bg-surface-container-low border border-outline-variant/20 p-5` with clip-path (clipped-corner-bl)
- Title: "System Health" with gold square accent (w-1.5 h-1.5 bg-secondary)
- Processor Load: label + percentage (starts ~42%, fluctuates +/- 3) + 1px tall progress bar with primary-container fill and glow shadow
- Aether Consumption: label + percentage (starts ~89%, fluctuates +/- 2) + 1px tall progress bar with accent (#ff4655) fill and glow shadow
- Status text: "All systems operational within optimal arcane parameters."

**Token Latency card:**
- `bg-surface-container-low border border-outline-variant/20 p-5`
- Title: "Token Latency" with gold square accent
- 2x2 grid of metric boxes (bg-surface-container-highest p-3 border-l-2):
  - ARC_NET: starts 12ms, fluctuates +/- 3, border-primary-container
  - VAL_FEED: starts 8ms, fluctuates +/- 2, border-primary-container
  - GLOBAL_REACH (col-span-2): starts 144ms, fluctuates +/- 15, border-accent/50
- Values update via setInterval every 2000-3000ms with small random deltas

**Node Connections card:**
- `flex-grow bg-surface-container-low border border-outline-variant/20 p-5 flex flex-col`
- Title: "Node Connections" with gold square accent
- 5 connection entries matching Stitch exactly:
  1. PARIS-HUB-01: online (green dot with pulse, primary-container), link icon
  2. SEOUL-LNK-04: online (green dot with pulse, primary-container), link icon
  3. NYC-GATE-09: offline (red dot, accent/#ff4655), link_off icon
  4. TKO-DATA-11: locked (gray dot, outline, opacity-40), lock icon
  5. LND-STR-02: online (green dot with pulse, primary-container), link icon
- Node status cycles randomly every 5-8 seconds: one random node changes between online/syncing/offline states (per user decision). Use setInterval.
- Online nodes: green pulsing dot (w-2 h-2 rounded-full bg-primary-container with shadow glow and CSS pulse animation)
- Offline nodes: red dot (bg-accent)
- Locked nodes: gray dot (bg-outline), reduced opacity
- Bottom: "Scan_For_Nodes" button (decorative, no action)

All setInterval timers must be cleaned up in useEffect return function.
  </action>
  <verify>
    <automated>cd /Users/djbeatbug/RoadToMillion/token-for-granted && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function useBootSequence" src/components/screens/terminal/BootSequence.tsx
    - grep -q "BOOT_SEQUENCE" src/components/screens/terminal/BootSequence.tsx
    - grep -q "setInterval" src/components/screens/terminal/BootSequence.tsx
    - grep -q "export function TerminalSidebar" src/components/screens/terminal/TerminalSidebar.tsx
    - grep -q "PROTOCOL" src/components/screens/terminal/TerminalSidebar.tsx
    - grep -q "DASHBOARD" src/components/screens/terminal/TerminalSidebar.tsx
    - grep -q "export function SystemMonitor" src/components/screens/terminal/SystemMonitor.tsx
    - grep -q "System Health" src/components/screens/terminal/SystemMonitor.tsx
    - grep -q "Token Latency" src/components/screens/terminal/SystemMonitor.tsx
    - grep -q "Node Connections" src/components/screens/terminal/SystemMonitor.tsx
    - grep -q "PARIS-HUB-01" src/components/screens/terminal/SystemMonitor.tsx
    - grep -q "SEOUL-LNK-04" src/components/screens/terminal/SystemMonitor.tsx
    - grep -q "NYC-GATE-09" src/components/screens/terminal/SystemMonitor.tsx
  </acceptance_criteria>
  <done>useBootSequence hook generates sequential boot entries + ongoing logs. TerminalSidebar renders 4 icon navigation buttons. SystemMonitor renders 3 monitoring panels with live-updating metrics and node connection status.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Tactical Terminal route with console, command input, and full layout</name>
  <files>src/routes/terminal.tsx</files>
  <read_first>
    - tactical_terminal_animated/code.html (full page layout -- sidebar + main + right panel)
    - src/components/screens/terminal/BootSequence.tsx (useBootSequence hook)
    - src/components/screens/terminal/SystemMonitor.tsx (right panel)
    - src/components/screens/terminal/TerminalSidebar.tsx (left sidebar)
    - src/components/ui/TerminalLog.tsx (TerminalLog component for rendering entries)
    - src/routes/index.tsx (route pattern reference)
  </read_first>
  <action>
Replace the placeholder in `src/routes/terminal.tsx` with the full Tactical Terminal screen.

**Page layout** (matching Stitch reference -- 3 columns: sidebar + main + monitoring):
The page uses a flex layout spanning full viewport height minus header/footer.

Structure:
1. Outer container: `flex h-[calc(100vh-8rem)]` (accounting for Shell header/footer)

2. Left: `<TerminalSidebar />`

3. Center (main terminal): `flex-grow flex flex-col gap-4 p-8 relative`
   - Background decoration: grid-bg pattern (use inline style or add CSS class for dot grid background: `radial-gradient(circle at 2px 2px, rgba(58,73,74,0.15) 1px, transparent 0)` background-size 24px 24px)
   - Hex shimmer decorative elements (2 absolute positioned blurred circles, primary-container/5 and secondary/5, with blur-[120px] and blur-[100px])

   - **Console header** (flex justify-between items-center):
     - Left: cyan accent bar (w-2 h-8 bg-primary-container with shadow glow) + title "Command_Console.v01" (font-headline text-2xl font-bold italic uppercase) + subtitle "SECURE LINK: ESTABLISHED // ENCRYPTION: ARCANE-AES-256"
     - Right: two status badges:
       - "Active_Node: PAR-09" (bg-surface-container-high border-l-2 border-primary-container, text-[10px])
       - "Alerts: 02" (bg-accent/10 border-l-2 border-accent, text-accent text-[10px])

   - **Terminal output area** (flex-grow):
     - Container: `bg-surface-container-lowest/80 border border-outline-variant/20 p-1` with clip-path (clipped-corner-tr: polygon 0 0, 92% 0, 100% 8%, 100% 100%, 0 100%)
     - Faint watermark: "AI_CORE" in absolute top-right, text-8xl font-black opacity-5
     - Scrollable inner area using the boot sequence entries:
       - Call `useBootSequence()` hook to get entries
       - Render entries directly (NOT using TerminalLog component -- the terminal screen uses custom rendering with animation delays per the Stitch reference)
       - Each entry: `<div className="mb-4">` with timestamp in secondary color, source/level label in primary-container (INFO) or accent (WARNING/CRITICAL), message in on-surface
       - Entries appear with staggered animation (use inline style animation-delay per entry index, CSS opacity 0 -> 1 transition, or Framer Motion stagger)
       - Auto-scroll to bottom as new entries appear using useRef + scrollIntoView

   - **Command input bar** (h-16):
     - Container: `bg-surface-container-highest border border-outline-variant/30 flex items-center px-6 gap-4` with clip-path: polygon(0% 0%, 100% 0%, 100% 100%, 4% 100%, 0% 60%)
     - Prompt symbol: ">>" in primary-container font-bold text-lg
     - Text input: `bg-transparent border-none font-label text-primary uppercase placeholder:text-outline/40 tracking-widest text-lg`, placeholder "EXECUTE COMMAND..."
     - Blinking cursor: `w-2.5 h-6 bg-primary-container` with CSS blink animation (opacity 0/1 alternating every 500ms using `animation: blink 1s step-end infinite`)
     - EXECUTE_LINK button: `bg-accent/10 border border-accent/40 text-accent px-6 py-2 font-headline font-bold text-xs tracking-widest` with clip-path and hover:bg-accent hover:text-background

   - **Easter egg handler:** Use useState for the input value. On form submit or button click:
     - Add a new LogEntry to the boot sequence entries: `{ timestamp: currentTime, level: 'SUCCESS', source: 'CMD_EXEC', message: 'COMMAND ACKNOWLEDGED. PROCESSING... [NO_OP_MODE_ACTIVE]' }`
     - Clear the input
     - The useBootSequence hook should expose an `addEntry(entry: LogEntry)` function for this

4. Right: `<SystemMonitor />`

**CSS additions:** Add the blink keyframe and grid-bg either as inline styles or as a `<style>` tag within the component (or use Tailwind's animate-pulse as approximation for blinking). Prefer inline style object for the blink animation:
```css
@keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }
```
Can use a `<style>` JSX block or define via useEffect injecting a style tag.
  </action>
  <verify>
    <automated>cd /Users/djbeatbug/RoadToMillion/token-for-granted && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useBootSequence" src/routes/terminal.tsx
    - grep -q "TerminalSidebar" src/routes/terminal.tsx
    - grep -q "SystemMonitor" src/routes/terminal.tsx
    - grep -q "Command_Console" src/routes/terminal.tsx
    - grep -q "EXECUTE COMMAND" src/routes/terminal.tsx
    - grep -q "EXECUTE_LINK" src/routes/terminal.tsx
    - grep -q "COMMAND ACKNOWLEDGED" src/routes/terminal.tsx
    - grep -q "createFileRoute" src/routes/terminal.tsx
    - grep -q "AI_CORE" src/routes/terminal.tsx
  </acceptance_criteria>
  <done>Tactical Terminal page at /terminal renders sidebar navigation, boot sequence console with staggered log entries, command input with easter egg response, and system monitoring panels with live-updating metrics. Layout matches the Stitch reference.</done>
</task>

</tasks>

<verification>
- `pnpm dev` renders the Tactical Terminal at /terminal with 3-column layout
- Boot sequence plays: 8 log entries appear sequentially on first visit
- After boot, ongoing logs appear every 4-6 seconds
- Command input accepts text, EXECUTE_LINK button shows "COMMAND ACKNOWLEDGED" response
- System Health panel shows fluctuating processor load and aether consumption bars
- Token Latency metrics update every 2-3 seconds with small deltas
- Node Connections shows 5 nodes with status dots that cycle
- Left sidebar shows 4 icon buttons with PROTOCOL active
- TypeScript compiles without errors
</verification>

<success_criteria>
- /terminal page displays full 3-column layout matching Stitch reference
- Boot sequence animates 8 entries sequentially on page load
- Terminal console shows color-coded log entries (INFO=cyan, WARNING=gold, CRITICAL=red)
- Command input works and shows decorative easter egg response
- Right panel metrics update live with setInterval
- Node connections cycle between online/offline/syncing states
- Sidebar shows Dashboard, Arsenal, Archives, Protocol icons with Protocol active
</success_criteria>

<output>
After completion, create `.planning/phases/03-remaining-screens/03-03-SUMMARY.md`
</output>
