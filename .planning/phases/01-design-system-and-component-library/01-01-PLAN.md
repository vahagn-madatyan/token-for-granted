---
phase: 01-design-system-and-component-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - wrangler.jsonc
  - src/styles/app.css
  - src/styles/fonts.css
  - src/styles/scanlines.css
  - src/styles/glitch.css
  - src/lib/cn.ts
  - app.config.ts
  - src/router.tsx
  - src/routeTree.gen.ts
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-03
  - FOUN-06

must_haves:
  truths:
    - "pnpm dev starts without errors and serves the app on localhost"
    - "Tailwind CSS v4 compiles with all Neon Monolith color tokens available as utilities"
    - "Space Grotesk and Inter fonts are loaded from local woff2 files"
    - "wrangler.jsonc declares AI, D1, KV, and R2 bindings"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "tanstack"
    - path: "vite.config.ts"
      provides: "Vite build configuration with Cloudflare"
      contains: "cloudflare"
    - path: "wrangler.jsonc"
      provides: "Cloudflare Workers bindings"
      contains: "d1_databases"
    - path: "src/styles/app.css"
      provides: "Design tokens via Tailwind v4 @theme"
      contains: "@theme"
    - path: "src/styles/fonts.css"
      provides: "Font face declarations"
      contains: "Space Grotesk"
    - path: "src/styles/scanlines.css"
      provides: "Scanline overlay CSS animation"
      contains: "scanline"
    - path: "src/styles/glitch.css"
      provides: "Glitch effect CSS keyframes"
      contains: "glitch"
    - path: "src/lib/cn.ts"
      provides: "Tailwind class merge utility"
      exports: ["cn"]
  key_links:
    - from: "src/styles/app.css"
      to: "tailwindcss"
      via: "@import 'tailwindcss'"
      pattern: "@import.*tailwindcss"
    - from: "vite.config.ts"
      to: "wrangler.jsonc"
      via: "cloudflare plugin reads wrangler config"
      pattern: "cloudflare"
---

<objective>
Scaffold the TanStack Start project with Cloudflare Workers support, configure the full Neon Monolith design token system in Tailwind CSS v4, load self-hosted fonts, and create all foundational CSS (scanlines, glitch effects). This establishes the project skeleton that every subsequent plan builds on.

Purpose: Without this foundation, no component or layout can render correctly -- design tokens, fonts, and build tooling must exist first.
Output: A working dev server (`pnpm dev`) with Tailwind v4 compiling all Neon Monolith tokens, fonts loaded, CSS effects defined, and Cloudflare bindings configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/01-design-system-and-component-library/01-CONTEXT.md
@tactical_entry_protocol_animated/code.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold TanStack Start project with Cloudflare Workers and install all dependencies</name>
  <files>package.json, tsconfig.json, vite.config.ts, wrangler.jsonc, app.config.ts, src/router.tsx, src/routeTree.gen.ts</files>
  <read_first>
    - DESIGN.md (lines 1-110 for tech stack, directory structure, and wrangler.jsonc template)
    - tactical_entry_protocol_animated/code.html (lines 1-100 for Tailwind config reference)
  </read_first>
  <action>
1. Scaffold the project using `pnpm create cloudflare@latest . --framework=tanstack-start --no-deploy` (if it prompts, accept defaults). If the scaffolding tool is unavailable or fails, manually create the project structure.

2. After scaffolding, install additional dependencies:
   ```
   pnpm add motion clsx tailwind-merge class-variance-authority zod
   ```

3. Create/update `wrangler.jsonc` with these exact contents:
   ```jsonc
   {
     "name": "token-for-granted",
     "compatibility_date": "2026-03-20",
     "compatibility_flags": ["nodejs_compat"],
     "main": "src/server.ts",
     "ai": { "binding": "AI" },
     "d1_databases": [{
       "binding": "DB",
       "database_name": "token-for-granted-db",
       "database_id": "",
       "migrations_dir": "migrations"
     }],
     "kv_namespaces": [{
       "binding": "KV",
       "id": ""
     }],
     "r2_buckets": [{
       "binding": "ASSETS_BUCKET",
       "bucket_name": "token-for-granted-assets"
     }],
     "vars": {
       "AI_GATEWAY_ID": "token-for-granted-gateway",
       "ENVIRONMENT": "production"
     }
   }
   ```

4. Ensure `vite.config.ts` uses the `@opennextjs/cloudflare` or TanStack's cloudflare preset. The exact config depends on what the scaffolder generates, but it must include the cloudflare plugin. If the scaffolder uses `@tanstack/react-start/plugin` with cloudflare preset, that's correct.

5. Create `src/lib/cn.ts`:
   ```ts
   import { clsx, type ClassValue } from 'clsx'
   import { twMerge } from 'tailwind-merge'

   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs))
   }
   ```

6. Ensure `app.config.ts` exists with TanStack Start config pointing to cloudflare preset.

7. Create `src/router.tsx` with the TanStack Router configuration (routeTree import, createRouter export). The scaffolder may already create this.

Note: The scaffolder will generate several files. Preserve its generated structure but ensure the above modifications are applied. Do NOT fight the scaffolder's opinions on file layout -- adapt to what it generates.
  </action>
  <verify>
    <automated>cd /Users/djbeatbug/RoadToMillion/token-for-granted && pnpm install && pnpm dev --port 3000 &amp; sleep 5 && curl -s http://localhost:3000 | head -20 && kill %1 2>/dev/null; true</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "motion" in dependencies
    - package.json contains "clsx" in dependencies
    - package.json contains "tailwind-merge" in dependencies
    - package.json contains "class-variance-authority" in dependencies
    - package.json contains "zod" in dependencies
    - wrangler.jsonc contains "d1_databases"
    - wrangler.jsonc contains "kv_namespaces"
    - wrangler.jsonc contains "r2_buckets"
    - wrangler.jsonc contains "\"AI\""
    - src/lib/cn.ts contains "export function cn"
    - src/lib/cn.ts contains "twMerge"
    - pnpm dev starts without build errors
  </acceptance_criteria>
  <done>TanStack Start project scaffolded with all dependencies installed, wrangler.jsonc configured with AI/D1/KV/R2 bindings, cn utility created, and dev server starts successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Neon Monolith design tokens, self-hosted fonts, and CSS effects</name>
  <files>src/styles/app.css, src/styles/fonts.css, src/styles/scanlines.css, src/styles/glitch.css, public/fonts/</files>
  <read_first>
    - DESIGN.md (lines 112-162 for full color token list, lines 318-362 for animation strategy)
    - tactical_entry_protocol_animated/code.html (lines 10-158 for exact Tailwind config colors, animation keyframes, and CSS classes)
    - hex_tactical_protocol/DESIGN.md (lines 1-73 for design system spec: surface hierarchy, typography, elevation rules)
    - The file being modified: src/styles/app.css (if it exists after scaffolding)
  </read_first>
  <action>
1. Download font files to `public/fonts/`:
   - Space Grotesk (woff2): weights 400, 500, 600, 700 from Google Fonts CDN or fontsource. Use `curl` to download from `https://fonts.gstatic.com` or install `@fontsource/space-grotesk` and copy woff2 files.
   - Inter (woff2): weights 400, 500, 600, 700. Same approach.
   - If downloading proves difficult, use `@fontsource/space-grotesk` and `@fontsource/inter` npm packages and import them in CSS.

2. Create `src/styles/fonts.css`:
   ```css
   @font-face {
     font-family: 'Space Grotesk';
     src: url('/fonts/SpaceGrotesk-Regular.woff2') format('woff2');
     font-weight: 400;
     font-display: swap;
   }
   @font-face {
     font-family: 'Space Grotesk';
     src: url('/fonts/SpaceGrotesk-Medium.woff2') format('woff2');
     font-weight: 500;
     font-display: swap;
   }
   @font-face {
     font-family: 'Space Grotesk';
     src: url('/fonts/SpaceGrotesk-SemiBold.woff2') format('woff2');
     font-weight: 600;
     font-display: swap;
   }
   @font-face {
     font-family: 'Space Grotesk';
     src: url('/fonts/SpaceGrotesk-Bold.woff2') format('woff2');
     font-weight: 700;
     font-display: swap;
   }
   @font-face {
     font-family: 'Inter';
     src: url('/fonts/Inter-Regular.woff2') format('woff2');
     font-weight: 400;
     font-display: swap;
   }
   @font-face {
     font-family: 'Inter';
     src: url('/fonts/Inter-Medium.woff2') format('woff2');
     font-weight: 500;
     font-display: swap;
   }
   @font-face {
     font-family: 'Inter';
     src: url('/fonts/Inter-SemiBold.woff2') format('woff2');
     font-weight: 600;
     font-display: swap;
   }
   @font-face {
     font-family: 'Inter';
     src: url('/fonts/Inter-Bold.woff2') format('woff2');
     font-weight: 700;
     font-display: swap;
   }
   ```
   NOTE: If using @fontsource packages instead, replace the above with:
   ```css
   @import '@fontsource/space-grotesk/400.css';
   @import '@fontsource/space-grotesk/500.css';
   @import '@fontsource/space-grotesk/600.css';
   @import '@fontsource/space-grotesk/700.css';
   @import '@fontsource/inter/400.css';
   @import '@fontsource/inter/500.css';
   @import '@fontsource/inter/600.css';
   @import '@fontsource/inter/700.css';
   ```

3. Create `src/styles/app.css` with Tailwind v4 `@theme` block for all Neon Monolith tokens:
   ```css
   @import 'tailwindcss';
   @import './fonts.css';
   @import './scanlines.css';
   @import './glitch.css';

   @theme {
     /* Surface hierarchy - tonal layering, no borders */
     --color-surface: #060e20;
     --color-surface-container-low: #091328;
     --color-surface-container: #0f1930;
     --color-surface-container-high: #141f38;
     --color-surface-container-highest: #192540;
     --color-surface-bright: #1f2b49;
     --color-surface-variant: #192540;
     --color-surface-dim: #0a141e;

     /* Primary - Cyan energy */
     --color-primary: #8ff5ff;
     --color-primary-container: #00eefc;
     --color-primary-dim: #00deec;

     /* Secondary - Magenta */
     --color-secondary: #ff59e3;
     --color-secondary-container: #ad009b;

     /* Tertiary - Lime */
     --color-tertiary: #deffab;
     --color-tertiary-container: #abfc00;

     /* On-surface text */
     --color-on-surface: #dee5ff;
     --color-on-surface-variant: #a3aac4;
     --color-on-primary: #005d63;
     --color-on-secondary: #42003a;
     --color-on-tertiary: #436600;

     /* Outline */
     --color-outline: #6d758c;
     --color-outline-variant: #40485d;

     /* Error */
     --color-error: #ff716c;
     --color-error-dim: #d7383b;

     /* Accent (Valorant red) */
     --color-accent: #ff4655;

     /* Font families */
     --font-headline: 'Space Grotesk', system-ui, sans-serif;
     --font-body: 'Inter', system-ui, sans-serif;
     --font-label: 'Space Grotesk', system-ui, sans-serif;
     --font-mono: 'JetBrains Mono', ui-monospace, monospace;

     /* Neon glow shadows */
     --shadow-neon-cyan: 0 0 20px rgba(143, 245, 255, 0.3);
     --shadow-neon-cyan-lg: 0 0 40px rgba(143, 245, 255, 0.4);
     --shadow-neon-magenta: 0 0 20px rgba(255, 89, 227, 0.3);
     --shadow-neon-accent: 0 0 20px rgba(255, 70, 85, 0.4);
     --shadow-neon-accent-lg: 0 0 40px rgba(255, 70, 85, 0.8);

     /* Border radius - tactical sharp edges */
     --radius-sm: 2px;
     --radius-md: 4px;
     --radius-lg: 8px;

     /* Animation durations */
     --duration-fast: 150ms;
     --duration-normal: 300ms;
     --duration-slow: 500ms;
   }

   /* Base styles */
   body {
     font-family: var(--font-body);
     background-color: var(--color-surface);
     color: var(--color-on-surface);
     overflow-x: hidden;
   }

   h1, h2, h3, h4, h5, h6 {
     font-family: var(--font-headline);
   }

   /* Noise texture overlay utility */
   .noise-bg {
     background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
     opacity: 0.05;
   }

   /* Clip path utilities from Stitch designs */
   .clipped-tr { clip-path: polygon(0 0, 92% 0, 100% 15%, 100% 100%, 0 100%); }
   .clipped-bl { clip-path: polygon(0 0, 100% 0, 100% 100%, 8% 100%, 0 85%); }
   .clipped-full { clip-path: polygon(10% 0, 100% 0, 100% 70%, 90% 100%, 0 100%, 0 30%); }
   ```

4. Create `src/styles/scanlines.css`:
   ```css
   .scanline-overlay {
     background: linear-gradient(to bottom, transparent, rgba(143, 245, 255, 0.03) 50%, transparent);
     height: 100px;
     width: 100%;
     position: fixed;
     top: 0;
     left: 0;
     z-index: 60;
     pointer-events: none;
     animation: scanline 8s linear infinite;
   }

   @keyframes scanline {
     0% { transform: translateY(-100%); }
     100% { transform: translateY(100vh); }
   }

   @keyframes flicker {
     0%, 100% { opacity: 0.1; }
     50% { opacity: 0.15; }
     15%, 85% { opacity: 0.08; }
     20%, 80% { opacity: 0.12; }
   }

   .animate-flicker {
     animation: flicker 3s infinite;
   }
   ```

5. Create `src/styles/glitch.css`:
   ```css
   /* Glitch text effect - clip-path + color channel shift */
   .glitch {
     position: relative;
   }

   .glitch::before,
   .glitch::after {
     content: attr(data-text);
     position: absolute;
     top: 0;
     left: 0;
     width: 100%;
     height: 100%;
   }

   .glitch::before {
     color: #00eefc;
     clip-path: inset(0 0 60% 0);
     animation: glitch-top 2s infinite linear alternate-reverse;
   }

   .glitch::after {
     color: #ff4655;
     clip-path: inset(60% 0 0 0);
     animation: glitch-bottom 2.5s infinite linear alternate-reverse;
   }

   @keyframes glitch-top {
     0% { transform: translate(0); }
     20% { transform: translate(-2px, 2px); }
     40% { transform: translate(-2px, -2px); }
     60% { transform: translate(2px, 2px); }
     80% { transform: translate(2px, -2px); }
     100% { transform: translate(0); }
   }

   @keyframes glitch-bottom {
     0% { transform: translate(0); }
     20% { transform: translate(2px, -1px); }
     40% { transform: translate(-1px, 2px); }
     60% { transform: translate(1px, 1px); }
     80% { transform: translate(-2px, -1px); }
     100% { transform: translate(0); }
   }

   /* Hover-only glitch jitter */
   .glitch-hover:hover {
     animation: jitter 0.1s infinite;
   }

   @keyframes jitter {
     0% { transform: translate(0); }
     25% { transform: translate(2px, -1px); }
     50% { transform: translate(-1px, 2px); }
     75% { transform: translate(1px, 1px); }
     100% { transform: translate(0); }
   }

   /* Text glitch with color channel shift on hover */
   .text-glitch-hover:hover {
     animation: textGlitch 0.2s cubic-bezier(.25, .46, .45, .94) both infinite;
     text-shadow: 2px 0 #00eefc, -2px 0 #ff4655;
   }

   @keyframes textGlitch {
     0% { transform: translate(0); }
     20% { transform: translate(-2px, 2px); }
     40% { transform: translate(-2px, -2px); }
     60% { transform: translate(2px, 2px); }
     80% { transform: translate(2px, -2px); }
     100% { transform: translate(0); }
   }

   /* Pulse glow animation */
   @keyframes pulseGlow {
     0%, 100% { box-shadow: 0 0 20px rgba(255, 70, 85, 0.4); }
     50% { box-shadow: 0 0 40px rgba(255, 70, 85, 0.8); }
   }

   .animate-pulse-glow {
     animation: pulseGlow 2s ease-in-out infinite;
   }

   /* Bracket-in animation for HUD corners */
   @keyframes bracketIn {
     0% { transform: scale(1.5); opacity: 0; }
     100% { transform: scale(1); opacity: 0.4; }
   }

   .animate-bracket-in {
     animation: bracketIn 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
   }
   ```

6. Ensure the root CSS file (app.css) is imported in the app entry point. Check what the scaffolder generated for the main entry and wire the import there. Typically in `src/routes/__root.tsx` or a top-level component, ensure `import '~/styles/app.css'` (or the path alias the scaffolder uses).
  </action>
  <verify>
    <automated>cd /Users/djbeatbug/RoadToMillion/token-for-granted && grep -q "@theme" src/styles/app.css && grep -q "surface" src/styles/app.css && grep -q "Space Grotesk" src/styles/fonts.css && grep -q "scanline" src/styles/scanlines.css && grep -q "glitch" src/styles/glitch.css && echo "All CSS files valid"</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/app.css contains "@import 'tailwindcss'"
    - src/styles/app.css contains "@theme"
    - src/styles/app.css contains "--color-surface: #060e20"
    - src/styles/app.css contains "--color-primary: #8ff5ff"
    - src/styles/app.css contains "--color-secondary: #ff59e3"
    - src/styles/app.css contains "--color-tertiary: #deffab"
    - src/styles/app.css contains "--color-accent: #ff4655"
    - src/styles/app.css contains "--color-on-surface: #dee5ff"
    - src/styles/app.css contains "--font-headline: 'Space Grotesk'"
    - src/styles/app.css contains "--font-body: 'Inter'"
    - src/styles/app.css contains "--shadow-neon-cyan"
    - src/styles/app.css contains "clipped-tr"
    - src/styles/app.css contains "clipped-bl"
    - src/styles/app.css contains "noise-bg"
    - src/styles/fonts.css contains "Space Grotesk" or fontsource import for space-grotesk
    - src/styles/fonts.css contains "Inter" or fontsource import for inter
    - src/styles/scanlines.css contains "scanline-overlay"
    - src/styles/scanlines.css contains "@keyframes scanline"
    - src/styles/glitch.css contains "@keyframes glitch-top"
    - src/styles/glitch.css contains ".glitch-hover"
    - src/styles/glitch.css contains ".animate-pulse-glow"
    - src/styles/glitch.css contains ".animate-bracket-in"
    - Fonts are either in public/fonts/ as woff2 files OR installed via @fontsource packages
  </acceptance_criteria>
  <done>All Neon Monolith design tokens configured in Tailwind v4 @theme, fonts loaded (Space Grotesk for headlines, Inter for body), scanline overlay animation defined, glitch CSS effects with clip-path color channel shift ready, clip-path utilities created, and CSS is importable by the root layout.</done>
</task>

</tasks>

<verification>
1. `pnpm dev` starts without errors
2. `grep -c "@theme" src/styles/app.css` returns at least 1
3. All 30+ color tokens are defined in app.css @theme block
4. Font files exist (either in public/fonts/ or via @fontsource packages)
5. CSS animation keyframes compile without errors
6. wrangler.jsonc contains all 4 Cloudflare bindings (AI, D1, KV, R2)
</verification>

<success_criteria>
- Dev server starts and serves a page at localhost
- Tailwind v4 is configured with the full Neon Monolith color palette (surface hierarchy, primary/secondary/tertiary, on-surface variants, outline, error, accent)
- Space Grotesk and Inter fonts are available for rendering
- Scanline, glitch, flicker, and pulse-glow CSS animations are defined and ready to use
- Cloudflare bindings are declared in wrangler.jsonc
- cn() utility is available for class merging
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-and-component-library/01-01-SUMMARY.md`
</output>
